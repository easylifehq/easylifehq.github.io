# EasyLife Next Tasks - Stage 34 Real Push Notification Gate

Stage 34 plan verdict: `STAGE_34_REAL_PUSH_NOTIFICATION_GATE_READY`.

Plan: `docs/codex/EASYLIFE_STAGE_34_REAL_PUSH_NOTIFICATION_PLAN.md`

AI assistant work remains parked at `READY_FOR_ONE_SYNTHETIC_PROVIDER_TEST_DEPLOY`. These tasks are for real push notifications only.

- [ ] User pain: EasyLife currently has local timer notifications, not true push notifications that can reach the phone when the app is closed. Skill/workflow: architecture, mobile-web, security-and-hardening. Target files: `docs/codex/EASYLIFE_STAGE_34_PUSH_CAPABILITY_AUDIT.md`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: audit current notification code, browser/PWA constraints, Firebase Messaging requirements, iPhone Home Screen requirements, and exact gaps before implementation. First-screen rule: no UI change. Remove/simplify requirement: separate local timer reminders from true push in plain language. Guardrails: docs only; no deploy, no live push, no Firebase config changes, no dependency/package changes, no generated output, no secrets, no token storage, no AI, no hidden writes, no external actions. Acceptance command: `npm.cmd run build` from `app-vNext`; `npm.cmd --prefix functions run lint`. Proof artifact: audit ends with `READY_FOR_PUSH_CLIENT_TOKEN_CONTRACT` or `NOT_READY_FOR_PUSH_CLIENT_TOKEN_CONTRACT`. Stop condition: stop if the implementation path requires a paid/external provider decision beyond Firebase Cloud Messaging.

- [ ] User pain: EasyLife needs a safe client token registration contract before any push can be sent. Skill/workflow: frontend-contract, privacy-and-security. Target files: `app-vNext/src/lib/mobile/pushNotifications.ts`, `app-vNext/src/lib/mobile/pushNotifications.test.ts` if test pattern exists, `app-vNext/src/features/settings/routes/SettingsPage.tsx`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: add disabled-by-default local contracts for FCM capability, permission state, VAPID public-key presence, token registration states, and user-facing status copy without storing a real token yet. First-screen rule: Settings notification page must say whether this device can attempt push. Remove/simplify requirement: do not mix local timer reminders and true push in one vague label. Guardrails: no deploy, no live push, no token writes, no backend changes, no secrets, no private keys, no dependency/package changes, no generated output, no hidden writes, no external actions. Acceptance command: `npm.cmd run build` from `app-vNext`; `npm.cmd --prefix functions run lint`. Proof artifact: MAGIC_SCORECARD records supported/unsupported/permission-denied/unconfigured states. Stop condition: stop if Firebase Messaging requires adding a dependency or frontend secret.

- [ ] User pain: A real push system must know which signed-in device may receive push without storing private app content. Skill/workflow: data-contract, Firebase, security-and-hardening. Target files: `docs/codex/EASYLIFE_STAGE_34_PUSH_TOKEN_STORAGE_CONTRACT.md`, `firestore.rules` only if a tiny explicit rules proposal is required, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: define the Firestore token record shape, ownership rules, redaction rules, delete/disable behavior, and metadata-only logging before implementation. First-screen rule: no UI change. Remove/simplify requirement: token records must not contain task/note/calendar/contact/workout text. Guardrails: docs/rules proposal only; no live token writes, no deploy, no live push, no secrets, no generated output, no AI, no broad notification scheduling. Acceptance command: `npm.cmd run build` from `app-vNext`; `npm.cmd --prefix functions run lint`. Proof artifact: storage contract proves user-owned token records and deletion path. Stop condition: stop if rules cannot enforce user ownership cleanly.

- [ ] User pain: Server push send behavior needs a narrow, disabled-by-default function before any real notification can be triggered. Skill/workflow: Firebase Functions, security-and-hardening. Target files: `functions/index.js`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: scaffold a separate `sendPushTestNotification` function that requires POST, verified Firebase auth, exact synthetic test payload, per-user throttle contract, kill switch, metadata-only logging, and disabled/fallback response by default. First-screen rule: no UI change. Remove/simplify requirement: do not connect task/calendar/workout reminders yet. Guardrails: no deploy, no live push, no broad scheduling, no AI, no old AI endpoint changes, no secrets printed/committed, no dependency/package changes, no generated output, no hidden writes except optional metadata-only proof fields if explicitly required. Acceptance command: `npm.cmd run build` from `app-vNext`; `npm.cmd --prefix functions run lint`. Proof artifact: function returns disabled/fallback until Stage 35 deploy/test. Stop condition: stop if the function would send push during tests/build or require deploying other functions.

- [ ] User pain: Before deploying push, EasyLife needs blunt proof that the lane is narrow, disabled, and phone-test ready. Skill/workflow: proof, mobile-web, security-and-hardening. Target files: `docs/codex/EASYLIFE_STAGE_34_REAL_PUSH_NOTIFICATION_PROOF_PACKET.md`, `docs/codex/CHECKPOINT_REVIEW.md`, `docs/codex/SIMON_DESIGN_REVIEW.md`, `docs/codex/ROBIN_COPY_REVIEW.md`, `docs/codex/MAGIC_SCORECARD.md`, `docs/codex/PHASE_STATE.md`, `docs/codex/NEXT_5_TASKS.md`. Change: prove capability audit, client token contract, token storage contract, disabled server send scaffold, no secrets, no live push, no automatic reminders, and no AI/external-action expansion. First-screen rule: inspect `/app/settings?section=notifications` if UI changed. Remove/simplify requirement: do not create recurring reminder tasks yet. Guardrails: docs/proof only unless tiny route-breaking repair is required; no deploy, no live push, no secret values, no generated output, no AI expansion. Acceptance command: `npm.cmd run build` from `app-vNext`; `npm.cmd --prefix functions run lint`. Proof artifact: end with `READY_FOR_STAGE_35_ONE_SYNTHETIC_PUSH_TEST_DEPLOY` or `NOT_READY_FOR_PUSH_TEST`. Stop condition: end not-ready if token ownership, kill switch, logging, fallback, or iPhone/PWA constraints are unclear.
