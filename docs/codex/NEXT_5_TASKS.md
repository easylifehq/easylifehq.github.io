# EasyLife Next Tasks - Stage 33 One Synthetic Provider Test

Stage 33 readiness verdict: `READY_FOR_ONE_SYNTHETIC_PROVIDER_TEST_DEPLOY`.

Proof packet: `docs/codex/EASYLIFE_STAGE_33_FIRST_PROVIDER_CALL_READINESS_PROOF_PACKET.md`

These are manual operator tasks, not background automation tasks. Do not run them unless the user explicitly asks to proceed with the one-call provider test.

Stage 33 must keep parked: broad chat, broad app context, real private user data by default, frontend provider keys, committed secrets, old endpoint expansion, hidden reads, hidden writes, automatic saves, email/text/call/message sending, scheduling, syncing, notifications, calendar changes, real memory, geocoding, device location, external actions, saved-object expansion, production rollout, and any route beyond `/app/easylist/add?demo=1`.

- [ ] User pain: The narrow provider path exists but has not been deployed for a real one-call test. Skill/workflow: release-operator, security-and-hardening. Target files: no repo files unless proof docs need update after the test. Change: confirm working tree is clean, confirm Firebase project `pipeline-2f422`, confirm `OPENAI_API_KEY` exists without printing it, and deploy only `functions:assistantIntakeSuggestion`. First-screen rule: no UI change. Remove/simplify requirement: do not deploy old AI helpers. Guardrails: no real user data, no frontend key, no broad deploy, no secret printing, no old endpoint expansion. Acceptance command before deploy: `npm.cmd run build` from `app-vNext`; `npm.cmd --prefix functions run lint`. Proof artifact: record deploy command and function name only, not secret values. Stop condition: stop if Firebase CLI would deploy `analyzeTaskBrainDump`, `planProjectWithAi`, hosting, rules, or any non-target function. Metadata: class:operator risk:critical mode:manual impact:ai-assistant surface:assistantIntakeSuggestion scope:firebase-functions.

- [ ] User pain: A deployed provider path must stay closed until the operator deliberately opens it for one synthetic request. Skill/workflow: release-operator, security-and-hardening. Target files: no repo files unless proof docs need update after the test. Change: set `ASSISTANT_INTAKE_PROVIDER_ENABLED=true` only for the test window and verify `ASSISTANT_INTAKE_PROVIDER_KILL_SWITCH` is not `true`. First-screen rule: no UI change. Remove/simplify requirement: close the gate immediately after one request. Guardrails: do not commit env files, do not print secrets, do not leave the lane open after the test. Acceptance command: metadata-only environment confirmation. Proof artifact: record enabled/disabled timestamps without secret values. Stop condition: stop if env setup requires committing config or exposing a provider key. Metadata: class:operator risk:critical mode:manual impact:ai-assistant surface:provider-gate scope:firebase-functions.

- [ ] User pain: The first request must prove provider behavior without touching private data. Skill/workflow: QA, security-and-hardening. Target files: no repo files unless proof docs need update after the test. Change: obtain a Firebase ID token for a signed-in test account without saving it, then send exactly one POST request using synthetic typed capture beginning with `[synthetic]`, metadata `source: operator-test`, metadata `reviewMode: synthetic-demo`, `liveCallRequested: true`, and the exact operator confirmation phrase. First-screen rule: no UI change. Remove/simplify requirement: do not use Inbox UI unless it has been explicitly wired for the operator switch later. Guardrails: no raw private text, no exact addresses, no real contacts, no real calendar text, no secrets in body/logs/docs. Acceptance command: one POST request only. Proof artifact: record response class, provider state, quarantine/fallback state, and `nothingSavedOrSent`; redact token and any raw provider text. Stop condition: stop if auth token, secret, or real private text would be copied into chat/docs/logs. Metadata: class:operator risk:critical mode:manual impact:ai-assistant surface:first-provider-request scope:manual-test.

- [ ] User pain: The test must not leave live AI open after the first response. Skill/workflow: rollback, security-and-hardening. Target files: no repo files unless proof docs need update after the test. Change: immediately set `ASSISTANT_INTAKE_PROVIDER_ENABLED=false` or `ASSISTANT_INTAKE_PROVIDER_KILL_SWITCH=true` after one request. First-screen rule: no UI change. Remove/simplify requirement: do not run a second request before proof review. Guardrails: no background retry, no repeated manual testing, no broad alpha rollout. Acceptance command: follow-up disabled/fallback request may be run only if it does not call provider. Proof artifact: record that the lane is closed. Stop condition: stop if disabling the gate fails. Metadata: class:operator risk:critical mode:manual impact:safety surface:provider-kill-switch scope:firebase-functions.

- [ ] User pain: EasyLife must learn from the first provider response before moving to private alpha. Skill/workflow: proof, product-review, security-and-hardening. Target files: `docs/codex/EASYLIFE_STAGE_33_FIRST_PROVIDER_CALL_RESULT_PROOF_PACKET.md`, `docs/codex/CHECKPOINT_REVIEW.md`, `docs/codex/SIMON_DESIGN_REVIEW.md`, `docs/codex/ROBIN_COPY_REVIEW.md`, `docs/codex/MAGIC_SCORECARD.md`, `docs/codex/PHASE_STATE.md`, `docs/codex/NEXT_5_TASKS.md`. Change: document whether the one synthetic provider call succeeded, fell back, or quarantined; verify no hidden writes/external actions; verify logs were metadata-only; decide whether Stage 34 can be private-alpha provider review. First-screen rule: inspect only `/app/easylist/add?demo=1` if UI changes. Remove/simplify requirement: do not create broad assistant expansion tasks unless the proof is strong. Guardrails: no second provider call, no deploy, no broad chat, no real memory, no external actions, no saved-object expansion. Acceptance command: `npm.cmd run build` from `app-vNext`; `npm.cmd --prefix functions run lint`. Proof artifact: proof ends `READY_FOR_STAGE_34_PRIVATE_ALPHA_PROVIDER_REVIEW` or `NOT_READY_FOR_STAGE_34`. Stop condition: end not-ready if logs, output validation, fallback, secret boundary, no-hidden-write, no-external-action, or gate closure proof is incomplete. Metadata: class:proof risk:critical mode:single impact:internal surface:first-provider-call-proof scope:docs/codex.
