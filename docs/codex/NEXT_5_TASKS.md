# EasyLife Next Tasks - Stage 33 First Provider Call Implementation

Stage 32 proof verdict: `READY_FOR_STAGE_33_FIRST_PROVIDER_CALL_IMPLEMENTATION`.

Stage 32 proof packet: `docs/codex/EASYLIFE_STAGE_32_NARROW_GATEWAY_PROOF_PACKET.md`

Stage 33 mission: implement the first real server-side provider call inside the narrow `assistantIntakeSuggestion` lane only.

Stage 33 must keep parked: broad chat, broad app context, real user data by default, frontend provider keys, committed secrets, old endpoint expansion, hidden reads, hidden writes, automatic saves, email/text/call/message sending, scheduling, syncing, notifications, calendar changes, real memory, geocoding, device location, external actions, saved-object expansion, production rollout, and any route beyond `/app/easylist/add?demo=1`.

- [ ] User pain: The server gateway exists but still returns only disabled fallback, so EasyLife still does not have a real model-backed Inbox suggestion. Skill/workflow: server-gateway, security-and-hardening. Target files: `functions/index.js`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: implement the first server-side OpenAI call inside `assistantIntakeSuggestion` only, using the existing server secret `OPENAI_API_KEY`, native `fetch` if available, the exact prompt `intake-suggestion`, and a tiny synthetic/demo typed-capture request shape. First-screen rule: no UI expansion. Remove/simplify requirement: do not touch or expand `analyzeTaskBrainDump` or `planProjectWithAi`. Guardrails: no deploy in this task unless separately requested, no frontend key, no raw secret printing, no broad context, no old endpoint expansion, no hidden write, no external action, no saved-object expansion. Acceptance command: `npm.cmd run build` from `app-vNext`; `npm.cmd --prefix functions run lint`. Proof artifact: docs record provider call path, secret boundary, prompt, route, no-hidden-write boundary, and whether any live call was actually made. Stop condition: stop if implementation requires a frontend key, package/dependency change, deploy config change, broad context, or old endpoint rewrite. Metadata: class:implementation risk:critical mode:single impact:ai-assistant surface:assistantIntakeSuggestion scope:functions,docs/codex.

- [ ] User pain: A raw provider response could be malformed or overclaim action if it is not shaped before returning to Inbox. Skill/workflow: output-validation, security-and-hardening. Target files: `functions/index.js`, `app-vNext/src/features/assistant/serverGateway/firstLiveProviderCallHarness.ts`, `app-vNext/src/features/assistant/serverGateway/firstLiveProviderCallHarness.test.ts`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: map provider output into the existing `stage-32-assistant-intake-response-v1` envelope and reject/fallback on hidden-write, external-action, real-memory, missing-source, missing-destination, unsupported-intent, or malformed output claims. First-screen rule: existing Inbox lane only. Remove/simplify requirement: keep the envelope small and do not add a second result panel. Guardrails: no deploy unless separately requested, no frontend provider key, no saved output, no sending/scheduling/syncing, no broad app context, no generated secrets, no package changes unless separately approved. Acceptance command: `npm.cmd run build` from `app-vNext`; `npm.cmd --prefix functions run lint`. Proof artifact: fixtures or documented checks for accepted, rejected, and fallback provider-style output. Stop condition: stop if unsafe provider output could render as saved or acted-on output. Metadata: class:implementation risk:critical mode:single impact:trust surface:provider-output-envelope scope:functions,app-vNext/src/features/assistant/serverGateway,docs/codex.

- [ ] User pain: The first live call needs a tiny controlled manual test instead of accidental production behavior. Skill/workflow: QA, security-and-hardening. Target files: `docs/codex/EASYLIFE_STAGE_33_FIRST_PROVIDER_CALL_TEST_PROTOCOL.md`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: create the exact manual test protocol for one synthetic Inbox capture, including allowed input, forbidden input, expected request, expected response, spend cap, rate limit, metadata-only logging, rollback, and immediate failure conditions. First-screen rule: no UI change. Remove/simplify requirement: do not create a broad AI test matrix. Guardrails: docs only; no live call, no deploy, no secrets, no SDKs, no package changes. Acceptance command: `npm.cmd run build` from `app-vNext`; `npm.cmd --prefix functions run lint`. Proof artifact: protocol ends with explicit operator checklist before any real test. Stop condition: stop if the protocol requires real user data or broad assistant behavior. Metadata: class:protocol risk:high mode:single impact:operator-safety surface:first-provider-call-test scope:docs/codex.

- [ ] User pain: After the provider path exists, the Inbox UI must show exactly what happened without making EasyLife feel like a broad autonomous assistant. Skill/workflow: frontend-copy, trust-ui. Target files: `app-vNext/src/features/easylist/routes/EasyListInboxPage.tsx`, `app-vNext/src/styles/globals.css`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/ROBIN_COPY_REVIEW.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: refine the existing first-live-call lane to show whether the response came from local fallback, function fallback, or one approved provider call, while keeping `nothing saved or sent` visible. First-screen rule: reuse existing assistant lane; do not add a new dashboard or large technical panel. Remove/simplify requirement: shorten one repeated warning if the new provider state adds density. Guardrails: no new save behavior, no broad chat, no external action, no real memory, no route expansion, no frontend key, no deploy config, no package changes. Acceptance command: `npm.cmd run build` from `app-vNext`; inspect `/app/easylist/add?demo=1`. Proof artifact: Robin copy note says the lane is understandable and does not overclaim. Stop condition: stop if UI implies autonomous work, saved output, sent message, scheduled reminder, calendar sync, real memory, geocoding, or broad AI availability. Metadata: class:implementation risk:high mode:single impact:user-trust surface:inbox-ai-lane scope:app-vNext/src/features/easylist,docs/codex.

- [ ] User pain: The first live provider implementation must be proven before expanding the assistant. Skill/workflow: proof, security-and-hardening, product-review. Target files: `docs/codex/EASYLIFE_STAGE_33_FIRST_PROVIDER_CALL_PROOF_PACKET.md`, `docs/codex/CHECKPOINT_REVIEW.md`, `docs/codex/SIMON_DESIGN_REVIEW.md`, `docs/codex/ROBIN_COPY_REVIEW.md`, `docs/codex/MAGIC_SCORECARD.md`, `docs/codex/PHASE_STATE.md`, `docs/codex/NEXT_5_TASKS.md`. Change: prove whether the first provider call path is safe, bounded, useful, and still approval-first. It must say whether a live provider call was actually made. First-screen rule: inspect only `/app/easylist/add?demo=1` if UI changed. Remove/simplify requirement: do not create Stage 34 expansion tasks unless proof is strong. Guardrails: proof only unless tiny route-breaking repair is required; no broad chat, no hidden writes, no external actions, no saved-object expansion, no frontend key, no old endpoint expansion, no production rollout. Acceptance command: `npm.cmd run build` from `app-vNext`; `npm.cmd --prefix functions run lint`. Proof artifact: proof ends `READY_FOR_STAGE_34_PRIVATE_ALPHA_PROVIDER_REVIEW` or `NOT_READY_FOR_STAGE_34`. Stop condition: end not-ready if provider output validation, fallback, logging, secret boundary, no-hidden-write, no-external-action, no-frontend-key, or route/prompt proof is incomplete. Metadata: class:proof risk:critical mode:single impact:internal surface:first-provider-call-proof scope:docs/codex.
