# EasyLife Next Tasks - Stage 26 First Live Provider Dry Run

Stage 25 proof verdict: `READY_FOR_STAGE_26_FIRST_LIVE_PROVIDER_DRY_RUN`.

Stage 25 proof: `docs/codex/EASYLIFE_STAGE_25_PROVIDER_READINESS_PROOF_PACKET.md`

Stage 26 mission: run the first tightly guarded model-provider dry run for Inbox typed-capture suggestion only, using synthetic/demo input only.

Stage 26 must keep parked: broad AI assistant behavior, real user data, frontend API keys, provider keys in docs/logs/commits, hidden reads, hidden writes, automatic saves, sending, scheduling, syncing, notifications, calendar changes, real memory, geocoding, device location, external actions, saved-object expansion, deploy changes, package/dependency changes, and production rollout.

- [ ] User pain: EasyLife cannot safely make a live provider call until the exact provider, server-side secret location, spend cap, logging policy, fallback behavior, first prompt, first route, and rollback plan are confirmed in one place. Skill/workflow: security-and-hardening, documentation-and-adrs, api-and-interface-design. Target files: `docs/codex/EASYLIFE_STAGE_26_PROVIDER_SECRET_CONFIRMATION.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: create the final Stage 26 confirmation note before implementation, naming the approved provider placeholder, server-side secret placeholder `SERVER_AI_PROVIDER_API_KEY`, spend cap, metadata-only logging rule, fallback rule, prompt ID `intake-suggestion`, route `/app/easylist/add?demo=1`, and rollback plan. First screen rule: not UI-facing; Inbox remains the only live dry-run route. Remove/simplify requirement: remove any ambiguity that the existing `pipeline-2f422` Firebase web config or any `VITE_` variable can be used as an AI provider secret. Guardrails: docs only; do not print or commit actual secrets; no SDKs, dependencies, backend implementation, deploy config, generated output, live calls, real user data, or external actions. Acceptance command: `npm.cmd run build` from `app-vNext`. Proof artifact: `docs/codex/EASYLIFE_STAGE_26_PROVIDER_SECRET_CONFIRMATION.md`. Stop condition: stop if provider choice or secret storage is still ambiguous. Metadata: class:planning risk:high mode:single impact:security surface:provider-secret-confirmation scope:docs/codex/EASYLIFE_STAGE_26_PROVIDER_SECRET_CONFIRMATION.md,docs/codex/MAGIC_SCORECARD.md.

- [ ] User pain: The first live provider call needs a server-only gateway path, not a browser key or direct provider request. Skill/workflow: api-and-interface-design, security-and-hardening, incremental-implementation. Target files: `app-vNext/src/features/assistant/serverGateway/serverGatewayLiveDryRunTypes.ts`, `app-vNext/src/features/assistant/serverGateway/serverGatewayLiveDryRun.ts`, `app-vNext/src/features/assistant/serverGateway/serverGatewayLiveDryRun.test.ts`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: implement a disabled-by-default server-shaped dry-run gateway contract for Inbox typed-capture suggestion that can call a provider only through a server-side seam and returns local fallback when disabled or unconfigured. First screen rule: no new UI yet; existing Inbox remains unchanged. Remove/simplify requirement: remove any possibility of browser-side provider credentials or direct browser provider calls. Guardrails: no frontend API key, no actual secret value, no real user data, no broad chat, no hidden reads/writes, no saves, no sending/scheduling/syncing/notifications/calendar changes, no real memory, no geocoding, no device location, no external actions, no deploy config, no package/dependency changes, no generated output. Acceptance command: `npm.cmd run build` from `app-vNext`. Proof artifact: dry-run tests showing disabled/unconfigured fallback and no direct provider call from browser modules. Stop condition: stop if implementation requires a provider SDK, package change, frontend secret, deploy change, or real user data. Metadata: class:feature risk:high mode:single impact:internal surface:server-gateway-dry-run scope:app-vNext/src/features/assistant/serverGateway,docs/codex/NIGHTLY_REPORT.md,docs/codex/MAGIC_SCORECARD.md.

- [ ] User pain: Real provider output must not appear in the UI unless Stage 20 validation accepts or downgrades it safely. Skill/workflow: test-driven-development, security-and-hardening. Target files: `app-vNext/src/features/assistant/serverGateway/serverGatewayLiveValidation.test.ts`, `app-vNext/src/features/assistant/serverGateway/serverGatewayLiveDryRun.ts`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: prove accepted, rejected, downgraded, timeout, disabled, invalid-request, validation-rejected, and provider-error cases for the live dry-run gateway path using synthetic fixtures only. First screen rule: no new UI; this is validation proof before render wiring. Remove/simplify requirement: keep output shapes limited to draft/preview/needs-review/fallback and reject broad assistant/action claims. Guardrails: no real user data, no raw payload logging, no hidden actions, no automatic retry, no saved-object expansion, no provider SDK/dependency/package/deploy/generated/secrets changes. Acceptance command: `npm.cmd run build` from `app-vNext`. Proof artifact: live dry-run validation tests and MAGIC_SCORECARD entry. Stop condition: stop if any provider output can bypass `validateAssistantModelOutput`. Metadata: class:proof risk:high mode:single impact:stability surface:provider-output-validation scope:app-vNext/src/features/assistant/serverGateway,docs/codex/NIGHTLY_REPORT.md,docs/codex/MAGIC_SCORECARD.md.

- [ ] User pain: The user needs to see the dry-run suggestion in Inbox without confusing it with saved work or a broad AI assistant. Skill/workflow: frontend-ui-engineering, security-and-hardening. Target files: `app-vNext/src/features/easylist/routes/EasyListInboxPage.tsx`, `app-vNext/src/features/assistant/serverGateway/serverGatewayLiveDryRun.ts`, `app-vNext/src/styles/globals.css`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/SIMON_DESIGN_REVIEW.md`, `docs/codex/ROBIN_COPY_REVIEW.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: add a compact Inbox dry-run source state for `Live provider dry run` using synthetic/demo capture only, with visible labels for source, prompt ID, validation state, fallback, and `nothing saved or sent`. First screen rule: keep Inbox compact and do not add another large technical panel if the existing server-adapter panel can be reused. Remove/simplify requirement: shorten or merge any duplicated no-live/no-save warning created by the new label. Guardrails: no real user data, no hidden writes, no changes to task/note save behavior, no broad AI chat, no email/calendar/notification/geocoding/device-location/external actions, no frontend keys, no secrets, no package/dependency/deploy/generated changes. Acceptance command: `npm.cmd run build` from `app-vNext`. Proof artifact: inspect `/app/easylist/add?demo=1` and report labels in NIGHTLY_REPORT, SIMON_DESIGN_REVIEW, and ROBIN_COPY_REVIEW. Stop condition: stop if the UI implies autonomous AI, real memory, sending, scheduling, syncing, or saved output. Metadata: class:feature risk:high mode:single impact:visible surface:inbox-live-dry-run scope:app-vNext/src/features/easylist/routes/EasyListInboxPage.tsx,app-vNext/src/features/assistant/serverGateway/serverGatewayLiveDryRun.ts,app-vNext/src/styles/globals.css,docs/codex.

- [ ] User pain: Before EasyLife can call itself an AI assistant, the first live-provider dry run needs a blunt proof packet that says what worked, what failed, and whether it is safe to continue. Skill/workflow: code-review-and-quality, security-and-hardening, documentation-and-adrs. Target files: `docs/codex/EASYLIFE_STAGE_26_FIRST_LIVE_PROVIDER_DRY_RUN_PROOF_PACKET.md`, `docs/codex/CHECKPOINT_REVIEW.md`, `docs/codex/SIMON_DESIGN_REVIEW.md`, `docs/codex/ROBIN_COPY_REVIEW.md`, `docs/codex/MAGIC_SCORECARD.md`, `docs/codex/PHASE_STATE.md`, `docs/codex/NEXT_5_TASKS.md`. Change: prove the first live-provider dry run used synthetic/demo input, server-only secret handling, Stage 20 context packets, Stage 20 prompt registry, Stage 20 output validation, local fallback, kill switch, spend cap, rate limit, metadata-only logging, rollback plan, and no hidden writes or external actions. First screen rule: proof may inspect Inbox but must not expand the first route beyond typed-capture suggestion. Remove/simplify requirement: remove any next task that jumps from dry-run to broad assistant behavior without proof. Guardrails: docs/proof only unless a tiny route-breaking repair is required; no broad AI assistant behavior, no real user data, no frontend keys, no secrets, no hidden writes, no external actions, no saved-object expansion, no deploy config, no package/dependency changes, no generated output. Acceptance command: `npm.cmd run build` from `app-vNext`. Proof artifact: `docs/codex/EASYLIFE_STAGE_26_FIRST_LIVE_PROVIDER_DRY_RUN_PROOF_PACKET.md`. Stop condition: end `NOT_READY_FOR_STAGE_27` if secrets, validation, fallback, logging, rate/spend, kill switch, or no-hidden-action proof is incomplete. Metadata: class:proof risk:high mode:single impact:internal surface:stage-26-proof scope:docs/codex/EASYLIFE_STAGE_26_FIRST_LIVE_PROVIDER_DRY_RUN_PROOF_PACKET.md,docs/codex/CHECKPOINT_REVIEW.md,docs/codex/SIMON_DESIGN_REVIEW.md,docs/codex/ROBIN_COPY_REVIEW.md,docs/codex/MAGIC_SCORECARD.md,docs/codex/PHASE_STATE.md,docs/codex/NEXT_5_TASKS.md.
