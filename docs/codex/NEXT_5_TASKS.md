# EasyLife Next Tasks - Stage 31 Separately Approved First Live AI Call

Stage 30 proof verdict: `READY_FOR_SEPARATELY_APPROVED_FIRST_LIVE_AI_CALL`.

Stage 30 proof packet: `docs/codex/EASYLIFE_STAGE_30_CONTROLLED_LIVE_AI_PROOF_PACKET.md`

Stage 31 mission: perform the first live AI provider call only after explicit human approval, and only for the Inbox typed-capture suggestion lane.

Stage 31 must keep parked: broad chat, real user data by default, frontend API keys, provider keys in docs/logs/commits, hidden reads, hidden writes, automatic saves, sending, scheduling, syncing, notifications, calendar changes, real memory, geocoding, device location, external actions, saved-object expansion, production rollout, broad app context export, and any route beyond `/app/easylist/add?demo=1`.

- [ ] User pain: A live AI call without a signed human approval record would turn the safety gate into vibes. Skill/workflow: security-and-hardening, documentation-and-adrs. Target files: `docs/codex/EASYLIFE_STAGE_31_FIRST_LIVE_AI_APPROVAL_RECORD.md`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: create the explicit approval record required before any provider call. It must name provider choice, server-side secret storage, secret placeholder, spend cap, rate limit, kill switch, metadata-only logging policy, first route `/app/easylist/add?demo=1`, first prompt `intake-suggestion`, allowed input class, fallback, rollback, and no-external-action boundary. First-screen rule: no UI change. Remove/simplify requirement: do not approve broad AI behavior. Guardrails: docs only; no live provider call, no provider SDK, no API key, no backend implementation, no deploy config, no package/dependency change, no generated output, no hidden write, no external action, no real memory, no notification, no calendar sync, no geocoding, no saved-object expansion. Acceptance command: `npm.cmd run build` from `app-vNext`. Proof artifact: approval record ending `APPROVED_FOR_ONE_SYNTHETIC_INBOX_PROVIDER_CALL` or `NOT_APPROVED_FOR_LIVE_AI`. Stop condition: stop if the user has not explicitly approved the provider, secret storage, spend/rate limits, kill switch, logging policy, and rollback plan. Metadata: class:approval risk:critical mode:single impact:security surface:first-live-ai-approval scope:docs/codex.

- [ ] User pain: Even after approval, the server-side secret boundary must be represented without putting a key in the repo. Skill/workflow: security-and-hardening, TypeScript-contracts. Target files: `app-vNext/src/features/assistant/serverGateway/liveProviderSecretBoundary.ts`, `app-vNext/src/features/assistant/serverGateway/liveProviderSecretBoundary.test.ts`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: add a TypeScript-only secret boundary contract that references `SERVER_AI_PROVIDER_API_KEY` by name, rejects `VITE_` provider secret names, rejects browser runtime access, and reports configured/unconfigured states without reading or printing a real key. First-screen rule: no UI change. Remove/simplify requirement: do not create a real env file or provider config. Guardrails: requires completed approval record; no real secret, no provider SDK, no provider call, no deploy config, no package/dependency change, no backend config, no generated output, no hidden write, no external action, no saved-object expansion. Acceptance command: `npm.cmd run build` from `app-vNext`. Proof artifact: tests showing server-only placeholder, unconfigured fallback, `VITE_` rejection, and browser access rejection. Stop condition: stop if implementation requires a real key, frontend key, env file edit, SDK, deploy config, or provider network call. Metadata: class:proof risk:critical mode:single impact:security surface:live-provider-secret-boundary scope:app-vNext/src/features/assistant/serverGateway,docs/codex.

- [ ] User pain: The first provider call must go through sanitizer, prompt registry, validation, quarantine, rate limits, and fallback instead of a direct fetch. Skill/workflow: security-and-hardening, TypeScript-contracts. Target files: `app-vNext/src/features/assistant/serverGateway/firstLiveProviderCallHarness.ts`, `app-vNext/src/features/assistant/serverGateway/firstLiveProviderCallHarness.test.ts`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: create a disabled-by-default harness for one synthetic Inbox typed-capture provider call. It must require the approval record, accept only sanitized request summaries, use prompt `intake-suggestion`, route output through response quarantine, preserve local fallback, and expose a no-op/unconfigured state when no server executor exists. First-screen rule: no UI change. Remove/simplify requirement: keep this harness narrow; do not add chat or broad context. Guardrails: no provider SDK, no frontend key, no committed secret, no automatic call on import/render, no hidden write, no external action, no saved-object expansion, no package/dependency change, no deploy config, no generated output. Acceptance command: `npm.cmd run build` from `app-vNext`. Proof artifact: tests for disabled, unconfigured, sanitized accepted request, sanitizer rejected request, quarantine rejected output, and fallback. Stop condition: stop if the harness would call a provider automatically, bypass sanitizer/quarantine, accept broad context, or expose secrets. Metadata: class:implementation risk:critical mode:single impact:security surface:first-live-provider-call-harness scope:app-vNext/src/features/assistant/serverGateway,docs/codex.

- [ ] User pain: A live AI result that cannot be audited will feel magical in the bad way. Skill/workflow: security-and-hardening, copy-review. Target files: `app-vNext/src/features/easylist/routes/EasyListInboxPage.tsx`, `app-vNext/src/features/assistant/serverGateway/firstLiveProviderCallHarness.ts`, `app-vNext/src/styles/globals.css`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/ROBIN_COPY_REVIEW.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: add compact Inbox copy/state labels for the separately approved first live call lane, showing source, prompt id, sanitizer state, quarantine/validation state, fallback state, and `nothing saved or sent`. It must remain hidden/disabled unless the harness reports an approved test state. First-screen rule: do not add a large new panel; reuse existing assistant lane chrome. Remove/simplify requirement: shorten one existing technical warning if this adds density. Guardrails: requires completed approval record; no broad assistant UI, no direct provider call from browser, no frontend key, no hidden write, no external action, no saved-object expansion, no deploy/package/dependency/generated output. Acceptance command: `npm.cmd run build` from `app-vNext`; inspect `/app/easylist/add?demo=1`. Proof artifact: route copy says first live call lane is approved/manual/suggestion-only and nothing saved/sent. Stop condition: stop if UI implies broad AI availability, autonomous work, saved output, sent message, real memory, scheduling, sync, geocoding, or device location. Metadata: class:implementation risk:high mode:single impact:user-trust surface:inbox-first-live-call-state scope:app-vNext/src/features/easylist,app-vNext/src/features/assistant/serverGateway,docs/codex.

- [ ] User pain: The first live AI call must be proven before EasyLife expands beyond one provider call. Skill/workflow: documentation-and-adrs, security-and-hardening, code-review-and-quality. Target files: `docs/codex/EASYLIFE_STAGE_31_FIRST_LIVE_AI_CALL_PROOF_PACKET.md`, `docs/codex/CHECKPOINT_REVIEW.md`, `docs/codex/SIMON_DESIGN_REVIEW.md`, `docs/codex/ROBIN_COPY_REVIEW.md`, `docs/codex/MAGIC_SCORECARD.md`, `docs/codex/PHASE_STATE.md`, `docs/codex/NEXT_5_TASKS.md`. Change: prove whether the first separately approved live AI provider call path is safe, bounded, auditable, and still suggestion-only. First-screen rule: inspect only `/app/easylist/add?demo=1` if UI changed. Remove/simplify requirement: do not create broad chat, memory, email, calendar, notification, geocoding, or production rollout tasks. Guardrails: proof only unless tiny route-breaking repair is required; no broad assistant behavior, no hidden write, no external action, no saved-object expansion, no frontend key, no committed secret, no deploy config, no package/dependency change, no generated output. Acceptance command: `npm.cmd run build` from `app-vNext`. Proof artifact: proof ends `READY_FOR_STAGE_32_PRIVATE_ALPHA_LIVE_AI_REVIEW` or `NOT_READY_FOR_STAGE_32`; if no provider call was actually made, say so explicitly. Stop condition: end `NOT_READY_FOR_STAGE_32` if approval, secret boundary, sanitizer, quarantine, logging, fallback, rollback, no-hidden-write, no-external-action, or no-frontend-key proof is incomplete. Metadata: class:proof risk:critical mode:single impact:internal surface:first-live-ai-call-proof scope:docs/codex.
