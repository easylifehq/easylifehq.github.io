# EasyLife Next Tasks - Stage 30 Controlled Live AI Gate

Stage 29 proof verdict: `READY_FOR_STAGE_30_CONTROLLED_LIVE_AI_GATE`.

Stage 30 mission: prepare the narrowest controlled path toward a separately approved first live provider call for the Inbox typed-capture suggestion lane.

Stage 30 must keep parked: broad chat, real user data by default, frontend API keys, provider keys in docs/logs/commits, hidden reads, hidden writes, automatic saves, sending, scheduling, syncing, notifications, calendar changes, real memory, geocoding, device location, external actions, saved-object expansion, deploy changes, package/dependency changes, generated output, provider SDKs, and production rollout.

- [ ] User pain: A live AI lane without a disabled-by-default environment contract could accidentally look enabled or leak provider assumptions into the browser. Skill/workflow: security-and-hardening, TypeScript-contracts. Target files: `app-vNext/src/features/assistant/serverGateway/liveAiEnvironment.ts`, `app-vNext/src/features/assistant/serverGateway/liveAiEnvironment.test.ts`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: define the disabled-by-default live AI environment contract, server-side secret placeholder name, allowed route `/app/easylist/add?demo=1`, allowed prompt `intake-suggestion`, provider call state labels, and explicit `VITE_` provider-secret prohibition. First-screen rule: no UI change. Remove/simplify requirement: remove or correct any wording in touched docs/tests that implies live AI is already enabled. Guardrails: no real secrets, no frontend API key, no provider SDK, no provider call, no backend config, no deploy config, no package/dependency change, no generated output, no hidden write, no external action, no saved-object expansion. Acceptance command: `npm.cmd run build` from `app-vNext`. Proof artifact: TypeScript contract fixture showing disabled-by-default server-only assumptions. Stop condition: stop if any implementation requires a real key, browser-exposed key, provider SDK, deploy config, or live network call. Metadata: class:proof risk:high mode:single impact:security surface:live-ai-environment scope:app-vNext/src/features/assistant/serverGateway,docs/codex.

- [ ] User pain: A provider request that carries too much app context would turn a narrow alpha into a privacy risk. Skill/workflow: security-and-hardening, TypeScript-contracts. Target files: `app-vNext/src/features/assistant/serverGateway/providerRequestSanitizer.ts`, `app-vNext/src/features/assistant/serverGateway/providerRequestSanitizer.test.ts`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: create a sanitizer that accepts only Inbox typed-capture suggestion requests, strips unnecessary app context, rejects secrets/auth/session data/contact details/exact addresses/note bodies/broad task lists/calendar contents/full context packets, and emits a provider-safe sanitized request summary. First-screen rule: no UI change. Remove/simplify requirement: keep sanitizer output small; do not create a broad app-context export. Guardrails: no provider call, no real user data by default, no secret, no frontend API key, no provider SDK, no backend config, no deploy config, no package/dependency change, no generated output, no hidden write, no external action, no saved-object expansion. Acceptance command: `npm.cmd run build` from `app-vNext`. Proof artifact: accepted/rejected sanitizer fixtures. Stop condition: stop if sanitizer allows broad app context, contact/private details, exact places, auth/session data, or raw secrets. Metadata: class:proof risk:high mode:single impact:privacy surface:provider-request-sanitizer scope:app-vNext/src/features/assistant/serverGateway,docs/codex.

- [ ] User pain: Unsafe provider-style output must be quarantined before it can render as a trusted suggestion. Skill/workflow: test-driven-development, security-and-hardening. Target files: `app-vNext/src/features/assistant/serverGateway/providerResponseQuarantine.ts`, `app-vNext/src/features/assistant/serverGateway/providerResponseQuarantine.test.ts`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: add a response quarantine path that uses Stage 20 output validation and quarantines malformed responses, hidden-write claims, external-action claims, real-memory claims, missing source/destination, unsupported intent, and ambiguous action wording before display. First-screen rule: no UI change. Remove/simplify requirement: reuse existing validator concepts; do not add another broad response schema. Guardrails: no provider call, no secret, no frontend API key, no provider SDK, no backend config, no deploy config, no package/dependency change, no generated output, no hidden write, no external action, no saved-object expansion. Acceptance command: `npm.cmd run build` from `app-vNext`. Proof artifact: quarantine fixture matrix with accepted/downgraded/quarantined outcomes. Stop condition: stop if unsafe provider-style output can render as accepted clean output. Metadata: class:proof risk:high mode:single impact:security surface:provider-response-quarantine scope:app-vNext/src/features/assistant/serverGateway,docs/codex.

- [ ] User pain: Turning on a live provider should require a human operator checklist, not vibes. Skill/workflow: documentation-and-adrs, security-and-hardening. Target files: `docs/codex/EASYLIFE_STAGE_30_PRIVATE_ALPHA_OPERATOR_CHECKLIST.md`, `docs/codex/NIGHTLY_REPORT.md`, `docs/codex/MAGIC_SCORECARD.md`. Change: create the private-alpha operator checklist covering provider choice, server-side secret storage, spend cap, rate limit, kill switch, metadata-only logging, no frontend key, first route, first prompt, fallback, rollback, and no external actions. First-screen rule: no UI change. Remove/simplify requirement: keep checklist short enough to actually use; do not approve broad AI behavior. Guardrails: docs only; no code, no real secrets, no provider SDK, no provider call, no backend config, no deploy config, no package/dependency change, no generated output, no hidden write, no external action, no saved-object expansion. Acceptance command: `npm.cmd run build` from `app-vNext`. Proof artifact: operator checklist with a clear `do not proceed if` section. Stop condition: stop if the checklist would permit broad chat, real memory, external actions, real user data by default, or frontend keys. Metadata: class:proof risk:high mode:single impact:operations surface:private-alpha-operator-checklist scope:docs/codex.

- [ ] User pain: EasyLife needs a final controlled-live-AI proof gate before any separately approved provider call, or the project will drift into unsafe live behavior. Skill/workflow: documentation-and-adrs, code-review-and-quality, security-and-hardening. Target files: `docs/codex/EASYLIFE_STAGE_30_CONTROLLED_LIVE_AI_PROOF_PACKET.md`, `docs/codex/CHECKPOINT_REVIEW.md`, `docs/codex/SIMON_DESIGN_REVIEW.md`, `docs/codex/ROBIN_COPY_REVIEW.md`, `docs/codex/MAGIC_SCORECARD.md`, `docs/codex/PHASE_STATE.md`, `docs/codex/NEXT_5_TASKS.md`. Change: create the Stage 30 proof packet verifying the live AI env contract, provider request sanitizer, provider response quarantine, private-alpha operator checklist, no frontend keys, no broad context, no hidden writes, no external actions, and local fallback availability. First-screen rule: proof may inspect Inbox but must not broaden beyond typed-capture suggestion. Remove/simplify requirement: remove any next task that jumps directly to broad chat, memory, email, calendar sync, or production rollout. Guardrails: docs/proof only unless tiny route-breaking repair is required; no live provider call, no provider SDK, no API key, no backend implementation, no deploy config, no package/dependency changes, no generated output, no hidden write, no external action, no real memory, no notification, no calendar sync, no geocoding, no saved-object expansion. Acceptance command: `npm.cmd run build` from `app-vNext`. Proof artifact: proof packet ending `READY_FOR_SEPARATELY_APPROVED_FIRST_LIVE_AI_CALL` or `NOT_READY_FOR_LIVE_AI`. Stop condition: end `NOT_READY_FOR_LIVE_AI` if any secret, context, validation, logging, rollback, fallback, operator approval, no-hidden-action, or no-frontend-key proof is incomplete. Metadata: class:proof risk:high mode:single impact:internal surface:stage-30-proof-and-live-ai-gate scope:docs/codex.
