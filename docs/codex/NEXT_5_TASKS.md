- [ ] User pain: EasyLife feels confusing because the current app exposes too many surfaces before the assistant shape is clear. Target: docs/codex/AI_ASSISTANT_STAGE_0_AUDIT.md. Change: inspect protected EasyLife app-vNext screens and write five lists: core surfaces to keep, support surfaces to keep quiet, optional modules to hide under More, confusing UI to delete/hide, and recent HQ changes to keep/revert/replace. Remove/simplify: do not add feature cards or dashboard sections; identify what should be removed or hidden next. Guardrails: docs-only; no product UI edits, no auth, no backend, no payments, no package or dependency changes, no release config, no generated output, no credentials, no unrelated ships. Acceptance: audit doc exists and names exact routes/files to simplify next. Check: git diff shows docs/codex changes only. [class:planning risk:low mode:single impact:docs scope:docs/codex/ accept:docs-only]
- [ ] User pain: EasyLife still reads like separate apps instead of one assistant. Target: docs/codex/AI_ASSISTANT_TARGET_IA.md. Change: define one-assistant navigation: Today, Inbox, Plan, Notes, More; include what each surface owns, what stays hidden, and what disappears from the first viewport. Remove/simplify: reject separate-app navigation as the primary mental model. Guardrails: docs-only; no product UI edits, no auth, no backend, no payments, no package or dependency changes, no release config, no generated output, no credentials, no unrelated ships. Acceptance: IA doc gives Stage 1 implementation slices with exact files/routes. Check: git diff shows docs/codex changes only. [class:planning risk:low mode:single impact:docs scope:docs/codex/ accept:docs-only]
- [ ] User pain: recent HQ changes may have preserved the wrong structure, so the next UI pass needs a reset decision before coding. Target: docs/codex/AI_ASSISTANT_HQ_RESET_DECISION.md. Change: inspect commits 068ccd27, bcbef141, and f0bad431 plus current HQ files, then recommend keep, revert, or replace for each change. Remove/simplify: do not solve the problem by color/theme alone. Guardrails: docs-only; do not run git revert; no product UI edits, no auth, no backend, no payments, no package or dependency changes, no release config, no generated output, no credentials, no unrelated ships. Acceptance: decision doc identifies the simplest safe first implementation move. Check: git diff shows docs/codex changes only. [class:planning risk:low mode:single impact:docs scope:docs/codex/ accept:docs-only]
- [ ] User pain: the fleet needs hard gates so it cannot turn the rebuild into another broad beauty pass. Target: docs/codex/AI_ASSISTANT_FLEET_GATES.md and docs/codex/AI_PERSONAL_ASSISTANT_REBUILD_PLAN.md. Change: confirm or tighten Stage 1 launch constraints: BatchSize 1, one owned surface per task, no new dashboard, every UI task removes or hides one confusing element, build required after UI changes. Remove/simplify: remove any vague "make it beautiful" instruction from the next actionable packet. Guardrails: docs-only; do not launch the fleet; no product UI edits, no auth, no backend, no payments, no package or dependency changes, no release config, no generated output, no credentials, no unrelated ships. Acceptance: docs contain enforceable Stage 1 rules. Check: git diff shows docs/codex changes only. [class:planning risk:low mode:single impact:docs scope:docs/codex/ accept:docs-only]
- [ ] User pain: implementation should not restart until Stage 1 has only small simplification slices. Target: docs/codex/NEXT_5_TASKS.md and docs/codex/TASK_QUEUE.md. Change: after audit, IA, and reset docs exist, replace NEXT_5_TASKS with exactly three Stage 1 UI tasks: assistant shell simplification, Today first viewport simplification, and More/optional module hiding. Remove/simplify: do not start Stage 2 visuals or add new modules. Guardrails: docs-only task planning; no auth, no backend, no payments, no package or dependency changes, no release config, no generated output, no credentials, no unrelated ships; each future UI task must require npm.cmd run build. Acceptance: exactly three Stage 1 implementation tasks are queued with exact files and forbidden scopes. Check: Select-String confirms three unchecked Stage 1 tasks after Stage 0 docs exist. [class:planning risk:low mode:single impact:docs scope:docs/codex/ accept:docs-only]
