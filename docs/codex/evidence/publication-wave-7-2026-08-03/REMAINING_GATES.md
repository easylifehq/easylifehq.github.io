# Remaining physical and approval gates

Publication automation is complete, but production deployment is intentionally blocked until every item is explicit:

- physical iPhone installed-PWA matrix;
- physical Android installed-PWA matrix;
- production custom-claim provisioning confirmation for the named paid-provider gates;
- exact production Firebase project ID and separately approved Firebase rules/three-Function scope;
- GitHub Pages source branch/folder and custom-domain settings;
- reviewed last-known-good rollback SHA;
- approved production Firebase client-configuration provisioning strategy. Review artifacts intentionally contain no production Firebase configuration and therefore disable authentication outside loopback demo mode;
- resolution or explicit acceptance of the HTML checkout-normalization mismatch before a generated-output branch is prepared;
- human production-deployment approval, operators, and window.

Wave 7 did not deploy, merge, provision claims, access real data, or run `--apply` against the real root.
